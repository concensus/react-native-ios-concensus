import React from 'react';
import {
    ScrollView,
    Text,
} from 'react-native';

const NewPollScreen = () => {
    return (
        <ScrollView>
            <Text>Propose a Motion</Text>
        </ScrollView>
    );
};

export default NewPollScreen;

